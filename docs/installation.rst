============
Installation
============

At the command line::

    $ easy_install django-mantis-openioc-importer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mantis-openioc-importer
    $ pip install django-mantis-openioc-importer